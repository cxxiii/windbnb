import * as React from 'react';
const Page = ({ children }) => (React.createElement("article", { className: "mb-16 text-gray-900 text-shadow-sm subpixel-antialiased" }, children));
export default Page;
