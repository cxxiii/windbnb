import * as React from 'react';
const Main = ({ children }) => (React.createElement("section", { className: "px-4 md:px-0 grid place-content-center md:container md:mx-auto" }, children));
export default Main;
